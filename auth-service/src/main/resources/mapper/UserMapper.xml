<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.prasac.authservice.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.prasac.authservice.entity.User">
        <id column="id" property="id" javaType="java.lang.Long" />
        <result column="username" property="username" javaType="java.lang.String" />
        <result column="password" property="password" javaType="java.lang.String" />
        <result column="email" property="email" javaType="java.lang.String" />
        <result column="full_name" property="fullName" javaType="java.lang.String" />
        <result column="enabled" property="enabled" javaType="java.lang.Boolean" />
        <result column="created_at" property="createdAt" javaType="java.time.LocalDateTime" />
        <result column="updated_at" property="updatedAt" javaType="java.time.LocalDateTime" />
    </resultMap>

    <select id="selectById" parameterType="java.lang.Long" resultMap="UserResultMap">
        SELECT id, username, password, email, full_name, enabled, created_at, updated_at
        FROM users
        WHERE id = #{id}
    </select>

    <select id="selectByUsername" parameterType="java.lang.String" resultMap="UserResultMap">
        SELECT id, username, password, email, full_name, enabled, created_at, updated_at
        FROM users
        WHERE username = #{username}
    </select>

    <select id="existsByUsername" parameterType="java.lang.String" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM users
            WHERE username = #{username}
        )
    </select>

    <select id="existsByEmail" parameterType="java.lang.String" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM users
            WHERE email = #{email}
        )
    </select>

    <insert id="insert" parameterType="com.prasac.authservice.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, email, full_name, enabled, created_at, updated_at)
        VALUES (#{username}, #{password}, #{email}, #{fullName}, #{enabled}, NOW(), NOW())
    </insert>

</mapper>
